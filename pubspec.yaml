name: doc_pipe
description: 一键式文档注释处理管道 —— 将多个文档处理子命令按固定顺序串行执行，
  统一接收输入路径，自动映射各阶段参数。
version: 1.0.0

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  path: ^1.9.1
  analyzer: ^6.4.1
  logger: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
